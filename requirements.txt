# FontMetricsNormalizer (ebrium) dependencies
# Font metrics normalization tool for family-wide metric consistency.

# Core dependencies
fonttools>=4.40.0
rich>=13.0.0

# WOFF2 support (fonttools uses Brotli to decompress WOFF2)
brotli>=1.0.9